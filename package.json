{
  "name": "lurexa",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@11.20.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @lurexa/learn-web dev",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "check-types": "pnpm -r check-types",
    "test": "pnpm -r test",
    "firebase:emulators": "pnpm --filter @lurexa/backend firebase:emulators"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
