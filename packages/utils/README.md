# `@lurexa/utils`

Shared utility functions for Lurexa packages and apps.

## Usage

```ts
import { cn, sleep, capitalize } from "@lurexa/utils";
```

## Scripts

- `pnpm lint` — Run ESLint
- `pnpm check-types` — Run TypeScript type checking
