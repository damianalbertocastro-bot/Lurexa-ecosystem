# `@lurexa/auth`

Authentication types and session helpers for Lurexa apps.

## Usage

```ts
import { createAuthContext } from "@lurexa/auth";
import type { Session } from "@lurexa/auth";
```

## Scripts

- `pnpm lint` — Run ESLint
- `pnpm check-types` — Run TypeScript type checking
