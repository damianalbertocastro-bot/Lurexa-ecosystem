{
  "schemaVersion": 2,
  "packages": {
    "@lurexa/backend": {
      "path": "packages/backend",
      "class": "Production",
      "role": "Shared implementation package. Root barrel is browser-safe; privileged capabilities are exposed only through explicit server-only subpaths."
    },
    "@lurexa/config": {
      "path": "packages/config",
      "class": "Contract",
      "role": "Shared product/domain/configuration contracts that must remain runtime-neutral."
    },
    "@lurexa/eslint-config": {
      "path": "packages/eslint-config",
      "class": "Test",
      "role": "Repository quality/tooling configuration."
    },
    "@lurexa/sdk": {
      "path": "packages/sdk",
      "class": "Contract",
      "role": "Typed cross-product/client contract helpers. It must not own authoritative persistence or privileged mutations."
    },
    "@lurexa/tokens": {
      "path": "packages/tokens",
      "class": "Contract",
      "role": "Canonical design-token contract shared across product personalities."
    },
    "@lurexa/types": {
      "path": "packages/types",
      "class": "Contract",
      "role": "Canonical shared TypeScript contracts and domain types."
    },
    "@lurexa/typescript-config": {
      "path": "packages/typescript-config",
      "class": "Test",
      "role": "Repository TypeScript/tooling configuration."
    },
    "@lurexa/ui": {
      "path": "packages/ui",
      "class": "Production",
      "role": "Shared browser-safe UI grammar and primitives."
    },
    "@lurexa/utils": {
      "path": "packages/utils",
      "class": "Production",
      "role": "Small runtime-neutral shared utilities."
    }
  },
  "retiredPackages": {
    "@lurexa/auth": {
      "formerPath": "packages/auth",
      "replacement": "Canonical product authentication and authorization live in governed app/Core/backend boundaries."
    },
    "@lurexa/database": {
      "formerPath": "packages/database",
      "replacement": "Core-owned backend repositories and trusted persistence boundaries replace the legacy generic database scaffold."
    }
  }
}
