# `@lurexa/tokens`

Design tokens for colors, spacing, and typography used across Lurexa apps.

## Usage

```ts
import { colors, spacing, typography } from "@lurexa/tokens";
```

## Scripts

- `pnpm lint` — Run ESLint
- `pnpm check-types` — Run TypeScript type checking
