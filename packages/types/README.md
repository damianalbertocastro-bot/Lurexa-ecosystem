# `@lurexa/types`

Shared TypeScript types used across Lurexa packages and apps.

## Usage

```ts
import type { User, ApiResponse } from "@lurexa/types";
```

## Scripts

- `pnpm lint` — Run ESLint
- `pnpm check-types` — Run TypeScript type checking
